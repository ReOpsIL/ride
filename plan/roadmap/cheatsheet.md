# Cheat sheet popup (2026-09-10)

Status (2026-09-24): implemented and expanded to a full reference. Sheets: Rust 89 sections / 2034 entries, C++ 87 / 1711, C 21 / 399, Makefile 14 / 242, CMake 7 / 71, TOML 5 / 52. Engine tests in `tests/cheatsheet.rs`, app tests in `app/RideTests/CheatSheetRowsTests.swift`, demo scene `cheatsheet`.

Goal of the 2026-09-24 expansion: a user with little Rust or C++ should see every option on the table. Each construct is listed in every form a beginner would type (typed and untyped bindings, mutable, nested, const and static, generic, with lifetimes) next to the idiomatic version, and each std API a beginner reaches for has an entry. Keys carry beginner synonyms (`list`, `fixed`, `declare`, `size`) because lookup is prefix-only.

Goal: while the user types in a Rust, C, C++ or Makefile buffer, a second popup shows the part of the language cheat sheet that fits the caret (items at file level, statements in a body, patterns in a `match` arm, functions inside `$(`), each entry a template the user can insert as a snippet. The sheet is data, not code: one TOML file per section under `cheatsheets/<lang>/`, embedded in the engine, so extending the sheet never touches Rust or Swift.

## Model

| Piece | Where | Responsibility |
|---|---|---|
| `Context` | `src/highlight/context/` | Where the caret is syntactically: `Item`, `Body` (impl/trait/class body), `Fields` (struct/enum body), `Statement`, `Expression`, `Type`, `Pattern`, `Attribute`, `Preprocessor`, `Use`, `Recipe`, `Function`, `Value`, `Unknown`. One detector per grammar (`rust.rs`, `c.rs` for C and C++, `make.rs`), wired through `Grammar.context` like the site classifier, tree-sitter ancestors first and a brace-depth text scan when the caret sits in an `ERROR` node. |
| Sheet data | `cheatsheets/<lang>/<section>.toml` | `title`, `contexts = [...]` (empty or omitted means any), `[[entries]]` with `name`, `keys` (lowercase match words), `doc` (one sentence), `snippet` (tab-stop syntax `${1:text}` / `$0`, four-space indent, first line unindented). A file stays at or under about 220 lines; a section that outgrows it is split into a new file with its own title and registered next to its parent. |
| Placeholders | `src/cheatsheet/stops.rs` | Mirrors the app's `SnippetParser`: a placeholder `${n:text}` ends at the first unescaped `}`, a literal `}` inside it is written `\}`, `${` without digits is literal (CMake `${VAR}`), and placeholders do not nest. The loader rejects an unclosed stop and a placeholder whose braces are unbalanced, because the app would otherwise cut the placeholder short and leave the tail as literal text. |
| Loader | `src/cheatsheet/{model,load,sheets/*}.rs` | `include_str!` tables per language, parsed once into `Sheet` behind `OnceLock`. |
| Lookup | `src/cheatsheet/lookup.rs` | Sections whose `contexts` contain the caret context come first in file order; with a typed prefix every section is filtered to entries whose name, name words or keys start with it, and non-context sections with matches follow. `all = true` ignores the context. Snippets are indented like the caret line (`engine/snippets.rs::render`). |
| FFI | `Engine::cheat_sheet(session_id, cursor_byte, all) -> CheatSheetResponse` | Reuses the session's `site_at` (prefix, replace start, comment/string veto) and `context_at`; `Site::Attribute` / `Include` / `Directive` / `UsePath` override the context. Returns `sections`, `replace_start_byte`, `prefix`, `context`. |
| CLI | `ride-engine cheat <file> [--byte N \| --find anchor] [--typed text] [--all]` | Prints the context and sections so the sheet can be probed without the app. |
| App | `app/Ride/CheatSheet/` | `CheatSheetController` (singleton, auto/pinned modes, fetch with query-id gating), `CheatSheetPopup` (panel, table of header and entry rows, preview pane, footer), `CheatSheetRows` (flatten sections, step over headers), `CheatSheetPlacement` (stacked on the far side of the completion popup, else at the caret), `CheatSheetRowView`, `CheatSheetPreview`, `CheatSheetFetch`. `SnippetInsert` is shared with completion accept. |

## Interaction

- Auto mode (preference "Cheat sheet with completions", on by default): the sheet appears with the completion popup whenever the caret context yields entries, narrows with every keystroke, and hides with the popup.
- Pinned mode: `⌃⇧Space` opens the sheet on demand (also without completions) and keeps it while the caret moves; a second `⌃⇧Space` or `esc` closes it.
- Browsing is separate from inserting: arrows or a click select a row and the preview pane shows the full example; `↩`, `⇥`, a second click on the selected row or a double-click inserts. Insertion replaces the typed prefix with the template and starts a snippet session (tab stops, `⇥` / `⇧⇥`).
- Keys while the completion popup is also visible: the completion list owns plain keys until the user browses the sheet with `⌥↑` / `⌥↓` or a click; the sheet is then focused and `↑` / `↓` / `↩` act on it until the next keystroke. `⌥↩` inserts without focusing.
- Placement: below the completion popup when the popup is below the caret, above it when the popup sits above the caret; alone, like the completion popup at the caret.

## Sheets

| Language | Sections |
|---|---|
| Rust | functions (signature, parameters, returns); structs, enums and unions, impls, consts and aliases; control flow, loop variants, match and block forms, early exits; bindings and patterns, typed and nested bindings, match arm patterns; types (primitives, arrays and slices, Option and Result nesting, std utilities, traits and markers, generic bounds, impl and dyn forms); traits (definition, patterns and dispatch, method calls and trait objects); std trait impls (Display and friends, conversions and borrowing, ops and iteration, arithmetic, bitwise); closures (captures, returning and storing, sorting and iterator callbacks); iterators (sources, adapters, consumers, sort and group); error handling, Option and Result combinators, error types and main; ownership (moves and borrows, lifetimes, smart pointers and interior mutability); strings (build and convert, split and search, chars and bytes); Vec, arrays, slices, HashMap, sets and ordered maps, queues and heaps; modules and imports; attributes, derives and lints; macros, std macros, macro_rules fragments and repetition; format specifiers and arguments; threads, channels, locks, atomics; async, tokio runtime, async channels and streams; unsafe and raw pointers, memory layout and asm, FFI; tests, assertions, rustdoc; files and I/O, stdio, Read and Write traits, text parsing, paths, filesystem; process, environment, time; expressions, calls and paths, literals, compound literals, operators, bits and integer math |
| C++ | functions (parameters, returns, out-of-line definitions); namespaces and aliases, globals, enums; classes and structs, inheritance, using objects, data members, member functions, constructors, copy and move, operator overloading in and outside the class; control flow, switch, for loops, while and jumps; variables, typed variables, pointers and references, arrays, pairs and tuples and optionals; fundamental types, standard containers and utility types, template parameters, type traits; templates, variadic templates, template idioms and utilities, concepts and constrained auto; containers, maps and sets, vector, deque and list, stacks and queues, iterators, map operations and idioms; algorithms (searching, numeric, modifying, merging and heaps), range algorithms and views; unique_ptr, shared_ptr and weak_ptr, RAII; lambdas, lambda variants, functional utilities; strings, search and conversion, string idioms and characters; console I/O, output formatting, string streams and files, filesystem, stream operators; exceptions, exception classes, error codes; preprocessor, platform checks, macros and idioms, modules, linkage and attributes; threads, futures, coroutines, mutexes and condition variables, concurrency idioms, atomics, latches and semaphores; casts, numeric and string literals, expressions and initialization, built-in operators, arithmetic, bit manipulation |
| C | items and functions, control flow, types and declarations, pointers and arrays, structs unions enums, preprocessor, strings and memory, I/O and files, storage and qualifiers, C11 and C23 features, expressions and operators |
| Makefile | rules and targets, variables, automatic variables, functions, conditionals and directives, recipes and shell, special targets, pattern rules and VPATH |

## Tests

- `tests/cheatsheet.rs`: every sheet parses, every context name is valid, every snippet has balanced tab stops and non-empty name and doc; caret-marker fixtures per language assert the detected context; lookup narrows by prefix and ranks context sections first.
- `app/RideTests/CheatSheetRowsTests.swift`: rows flatten with headers, stepping skips headers, placement picks the far side of the completion frame.

## Limits

- Context detection is syntactic; it does not know the type of an expression, so `Expression` shows the same sections after `x.` regardless of `x`.
- Some places have no context of their own and fall back to a neighbour: a `where` clause and a generic parameter list read as `type`, a format string literal has no context (format sections use `contexts = []`), a `#[test]` body is a plain `statement`, and a builder chain (`.stderr(Stdio::null())`) is an `expression`.
- Lookup is prefix-only. Punctuation ends the word, so `[` or `std::` gives an empty prefix and the whole context section; `std::` names match once the user types past the colons.
- Tree-sitter error recovery folds a fragment into the next item or statement (`impl` before `struct S;` parses as `impl struct S`, `int main() { arr }` as a braced initializer). The detectors only classify an item header as `type` once the caret is past its keyword, and treat a braced initializer on a function declarator as a body.
