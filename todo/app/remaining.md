# App follow-ups (see plan/roadmap/improve-extend.md)

- CompletionPopup: hide on app deactivate / window resign key / tab switch
- Workspace tree: hide dotfiles by default, `showHidden` preference
- Add RideTests target: UTF-16 round-trip, stale query_id drop
- CI: xcodebuild build + test job
- StatusBar: show phase + warning count, not raw crate errors
